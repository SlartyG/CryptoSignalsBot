# Telegram
BOT_TOKEN=

# VPS (Docker): DATABASE_URL и REDIS_URL можно не указывать — подставит docker-compose.
# Mac (локально): host=localhost + docker-compose.dev.yml
# DATABASE_URL=postgresql+asyncpg://app:app@localhost:5432/cryptobot
# REDIS_URL=redis://localhost:6379/0

# Crypto Pay (Phase 5)
CRYPTO_PAY_TOKEN=

# Comma-separated Telegram user IDs
ADMIN_TELEGRAM_IDS=

# Metabase (optional)
POSTGRES_PASSWORD=app
# Admin credentials from Metabase first-time setup (for dashboard script)
METABASE_EMAIL=
METABASE_PASSWORD=
# METABASE_URL=http://metabase:3000
